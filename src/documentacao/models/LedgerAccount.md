# Tabela `ledger_accounts`

**Propósito:** contas sintéticas do razão (cliente, especialista, plataforma). `cached_balance` segue `DECIMAL(14,4)` para alinhar precisão com pedidos e ledger.

Ver [`Relacionamentos_FKs.md`](./Relacionamentos_FKs.md) para FKs lógicas (`client_id`, `specialist_id`).
